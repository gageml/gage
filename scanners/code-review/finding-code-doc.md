Code quality finding in a session

Each finding names a concrete, checkable defect in the project's code or
other artifacts. Verify the claim against the current project content. A
single verified instance is sufficient evidence for an issue; repetition is
not expected, since a fixed defect does not recur.
