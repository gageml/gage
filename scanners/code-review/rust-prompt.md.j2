Use the `mcp__gage__Query` tool to review the Rust code generated by the
assistant in session {{ session_id }}.

Look for code quality issues and report them using `mcp__gage__Issue`.

It's common for a model to attempt changes, detect error in the changes, and
then correct the code. If the model efficiently corrected itself, do not an
issue. However, if you spot a costly error detection-and-fix cycle (e.g. several
turns) or a repeating failure/correction pattern, report it as an issue.
