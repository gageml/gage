# Issue summary

The number of unresolved issues at the end of the scan. The note value
is the total; `metadata.open` and `metadata.pending` break it down by
status.

Counts cover the whole issue docket, not only the issues this scan
produced.
