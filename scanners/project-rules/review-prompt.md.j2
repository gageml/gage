You are reviewing session {{ session_id }} to check for rule compliance.

Read the conversation by querying the `message` table via `mcp__gage__Query`,
filtering by `session_id = '{{ session_id }}'`. `message` table columns:
(session_id, line, uuid, type, subtype, text, timestamp, attachments, ide_tags,
raw)

If you find evidence of a rule violation, `mcp__gage__NoteWrite` to write a note with
these paramters:

- `name` - always use "rule-violation"
- `value` - rule violation description --- include the rule and how it was violated
- `session_id` - always use "{{ session_id }}"
- `session_line` - the line number associated with the violation

Apply the following rules:

{{ rules }}
