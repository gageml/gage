Written for user or tool use interrupts.

- **value** : `user` for user interrupts, `tool_use` when user rejects a tool
  use prompt
- **target** : session and line of the interrupted in-progress turn

The note carries no information about why the user interrupted the turn.
