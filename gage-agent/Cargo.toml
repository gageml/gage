[package]
name = "gage-agent"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
gage-core = { path = "../gage-core" }
gage-db = { path = "../gage-db" }
libc = "0.2"
notify = "8"
serde_json = { workspace = true }
tokio = { workspace = true, features = ["process", "time", "rt", "io-util", "sync"] }
tracing = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
